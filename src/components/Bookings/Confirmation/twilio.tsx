export default function TextConfirmation() {
    // todo this needs to take in a date / number
    // send an immediate confirmation text
    // send a one week reminder text if the date is more than 7 days away
    // send a one day reminder text

    return (
        <div>
            <div></div>
        </div>
    );
}
