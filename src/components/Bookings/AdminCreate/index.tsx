interface AdminCreateBookingProps {
    closeModal: () => void;
    userId: string;
}

export default function AdminCreateBooking({
    closeModal,
    userId,
}: AdminCreateBookingProps) {
    return (
        <>
            <div>hello there</div>;
        </>
    );
}
